# 첫날.zip 에이전트 작업 규칙

## 시작 순서

매 반복에서 다음 순서로 읽는다.

1. `goal.md`
2. `prd.md`
3. `task.md`
4. `progress.md`
5. 관련 코드와 테스트

## 한 번의 반복

1. `task.md`에서 의존성이 충족된 P0 미완료 작업 하나를 선택한다.
2. 해당 작업의 첫 미완료 체크박스부터 진행한다.
3. 실패하는 테스트를 먼저 추가하거나 기존 검증 명령으로 실패를 재현한다.
4. 수용 기준을 만족하는 최소 구현을 작성한다.
5. 관련 테스트를 실행하고, 작업 경계에서 전체 테스트와 빌드를 실행한다.
6. 사용자 흐름 작업은 브라우저로 실제 동작을 확인한다.
7. 검증된 체크박스만 완료 처리하고 `progress.md`에 결과를 추가한다.
8. 다음 반복에서 문서를 다시 읽고 현재 상태를 재평가한다.

## 작업 원칙

- P0 수직 흐름을 완성하기 전에는 P1·P2 기능을 구현하지 않는다.
- 한 반복에서 여러 독립 기능을 동시에 시작하지 않는다.
- AI 출력과 결정론적 판정을 분리한다.
- 매장 규칙과 행사 정보를 UI·프롬프트·검증 코드에 중복 하드코딩하지 않는다.
- 사용자 노출 문구의 역할명은 반드시 `스토어 매니저`, `경영주`로 통일한다.
- 실제 GS25 데이터나 공식 브랜드 자산을 사용하지 않는다. 화면에 합성 데이터임을 표시한다.
- 비밀 값은 `.env.local`에서만 읽고 출력·커밋하지 않는다.
- `.env.local`은 수정해도 내용을 로그나 `progress.md`에 복사하지 않는다.
- 사용자 소유 변경을 덮어쓰거나 관련 없는 파일을 정리하지 않는다.

## 검증 규칙

- 테스트를 실행하지 않았으면 `통과`라고 기록하지 않는다.
- 로컬 빌드 통과는 배포 성공이나 실제 Gemini 연결 성공을 의미하지 않는다.
- Gemini 없이 통과한 검증은 `데모 모드` 검증으로 구분한다.
- 환경변수가 필요한 테스트는 키의 존재 여부만 확인하며 값을 출력하지 않는다.
- E2E는 최소한 경영주 규칙 수정, 잘못된 시도, POS 조회 후 재도전, Q&A, 체크리스트, 대시보드 흐름을 포함한다.

## 실패 및 중단 규칙

- 같은 원인의 실패가 세 번 반복되면 추측성 수정을 중단한다.
- `progress.md`에 명령, 핵심 오류, 시도한 수정, 필요한 사람의 결정을 기록한다.
- 권한, 유료 비용, 실제 데이터, 비밀 값 노출, 파괴적 명령이 필요한 경우 사람에게 요청한다.
- P0 수용 기준과 모든 필수 검증이 통과하면 Ralph 반복을 종료한다.

## 기록 형식

각 반복이 끝날 때 `progress.md`에 다음을 추가한다.

- 반복 번호와 시각
- 선택한 작업
- 변경 파일
- 실행한 검증과 실제 결과
- 실패와 수정 내용
- 알려진 제한 또는 차단 요인
- 다음 미완료 작업
