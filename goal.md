# Candidate Source Text for Ralph `/goal`

> After entering this into the event tool, update this file so it exactly matches the submitted source text, and record the reason for any changes in `progress.md`.

> Prepared for a 7–10 hour implementation run with a minimum useful-work target of 420 minutes. Copy the complete text block as the goal source. This document has not itself started a runner or changed any host limit. See `docs/ralph-long-run-audit.md` for the audit and launch checklist.

```text
Goal: Complete a working web app MVP where a new `스토어 매니저` can practice handling customer questions about a 2+1 promotion before their first shift, use AI Q&A and a checklist grounded in the same store rules during work, and where the `경영주` can review training, questions, task progress, and areas where the manual needs improvement.

Target end state: When the `경영주` saves the virtual store rules and checklist, a new `스토어 매니저` responds to an AI customer's 2+1 promotion inquiry, checks the virtual POS, and receives feedback and a retry based on their action log. The user then uses rule-grounded Q&A and a checklist in work mode, and the `경영주` reviews the results in the dashboard. Rule edits must apply to subsequent new Q&A requests and new simulation sessions.

Execution: Read AGENTS.md once at startup, then begin every iteration by reading goal.md, prd.md, task.md, progress.md, and relevant code/tests in that order. Select one incomplete P0 task whose explicit dependencies are satisfied, beginning with its first incomplete checkbox. Reproduce the problem or write a failing test, implement the smallest coherent solution, and actually run verification. At each task boundary run the full test suite and production build, plus browser verification for changed user flows. Record only verified checkboxes and continue to the next eligible task without waiting for routine approval. Use available tools directly; do not block on an unavailable skill or require parallel agents.

Computer-use at bottlenecks: When progress is blocked by browser or desktop UI behavior that code, logs, CLI tools, or automated tests cannot adequately diagnose or operate, use the available `computer-use` skill to inspect and resolve the bottleneck. Examples include focus problems, hidden controls, dialogs, and browser automation failures needing visual inspection. Read the installed skill's SKILL.md and required runtime guidance before use, verify a live controllable target, inspect its current state, perform the smallest authorized action, and verify the resulting state. Do not wait for three failures before switching to this evidence-gathering method; preserve the existing root-cause failure count. Record the bottleneck, actions, observed result, and evidence in progress.md, then return to normal implementation and automated verification. A screenshot alone is not proof that an app can be controlled. If the runtime is unavailable, use only its documented recovery path, record the exact blocker, and continue independent eligible P0 work; do not invent APIs or make the whole run depend on this skill. Computer use follows the same pre-authorized Gemini, Git, and deployment scope below without renewed confirmation. It does not replace required tests or expand authorization to unrelated actions, new paid purchases, or bypassing enforced host permissions and limits.

YOLO execution authorization: The user has pre-authorized real Gemini calls, deployment, and Git publication for this project, including local Git initialization, project commits, pushes, and deployment updates. Do not request separate confirmation. Use existing credentials and authenticated accounts, and resolve repository, branch, and deployment targets from actual project configuration; ask only for genuinely missing information or required login interaction. After local gates pass, publish and deploy when prerequisites are available, then verify the remote commit, final deployment status, and deployed browser flow. Bounded live Gemini smoke checks and normal API usage on the existing account are authorized, as is necessary server-side secret configuration on the verified deployment target; never print secret values or publish .env.local. Inspect staged files and preserve unrelated changes. No force-push, remote deletion, new paid subscription or plan upgrade, real data, or unrelated destructive operation is authorized by this paragraph. This authorization does not alter the host sandbox, enforced tool approval policy, or usage limits.

Duration and continuation: Aim for at least 420 minutes (7 hours) of measured active implementation and verification, with enough planned work for a 7–10 hour session. Tasks 1–9 deliver the core flow; continue to required P0 Tasks 10–16 for persistence, snapshot integrity, AI failure handling, manual/checklist completeness, accessibility, browser regression, and release evidence. Task 9 success is not the stopping point. Start the timer only when implementation starts, record timestamped active intervals in progress.md at iteration boundaries and at least every 30 minutes, and exclude idle/approval waits and stopped sessions. Preserve measured time and the exact next checkbox across resumes. Continue beyond 420 minutes when acceptance criteria remain incomplete. Do not fill time with sleeps, unchanged test reruns, invented work, or unauthorized P1/P2 features. If every required gate and one bounded residual review are complete before 420 minutes, record completed-before-duration-target and stop honestly; a prompt cannot guarantee host uptime or a minimum duration.

Mock-data reference: Whenever creating or revising the app's internal mock or seed data—including mock manual categories and rules, scenarios, Q&A tags, checklist items, and dashboard groupings—first read `docs/gs25-store-manager-training-map.md` and use its public education-map taxonomy and FirstDay.zip application notes as design inspiration. Use only the parts that support the existing P0 vertical flow, especially customer service, product and expiration-date management, virtual-POS verification behavior, checklist templates, and unresolved-question classification. This document is a development-time reference, not a runtime data source or an authoritative store manual: do not copy linked-video content, real POS screens, internal procedures, or potentially stale public material into the app. All mock facts, prices, promotions, procedures, and evaluation rules must remain synthetic and must come from the app's controlled structured data and manager-authored rules. Do not implement the document's P1 candidates before P0 is complete.

Core constraints: Use only synthetic data—never real GS25 operational data, POS data, internal systems, or personal information. Read the Gemini API key only on the server, and never expose it in the browser, logs, or Git. The LLM is responsible only for natural-language expression; structured data and deterministic code are responsible for product and promotion facts and action evaluation. Do not invent answers that are not grounded in the manual. Do not turn the checklist or dashboard into a surveillance or personnel-evaluation tool. Satisfy every P0 acceptance criterion before expanding the scope to P1 or P2.

Definition of done: (1) The flow from saving manager rules through the final dashboard can be demonstrated in one browser session. (2) The action log and feedback actually differ depending on whether a POS lookup occurred. (3) The virtual POS, validator, and Q&A use the same rules. (4) Rule edits apply to subsequent new sessions while existing sessions keep their immutable snapshot. (5) Manager-authored checklist items and state survive refresh and are aggregated accurately in the dashboard. (6) Unsupported questions are recorded as unresolved; adding a supporting rule affects a new question without silently rewriting history. (7) The app remains demonstrable in a clearly labeled demo mode without a Gemini key or API. (8) Unit, integration, the complete E2E suite against a production server, and the production build pass. (9) The README documents setup, demo flow, synthetic data, and limitations. (10) Every required checkbox in Tasks 1–16 and the PRD has actual evidence; persistence failures, event ordering, request failures, keyboard/mobile use, reset behavior, and server/client boundaries are verified. (11) The elapsed-work target and any early-stop exception are reported accurately.

Verification record: Accumulate commands, exit codes/results, regression fixes, browser flows/viewports, active-work intervals, and remaining limitations in progress.md. Link each acceptance criterion to a concrete test or browser evidence in docs/verification-matrix.md. Never report an unrun test, live API connection, or deployment as passed. Preserve the real Codex JSONL session log and do not fabricate its contents afterward. Before interruption, save a resume checkpoint with the exact next checkbox, latest command, unresolved root-cause failure counts, and any running server port/process.

Stopping and escalation: Stop when all extended P0 gates are verified and report the duration outcome, not merely when the first demo works or the clock reaches seven hours. Keep AI_DEMO_MODE=true for repeatable automated verification and its builds; use a separate live-mode run for the pre-authorized bounded Gemini smoke check, then restore demo mode for regressions. Missing Gemini credentials do not block demo-mode tasks. Proceed with pre-authorized Git publication and deployment after local gates pass. Record missing credentials, targets, login access, or tools as concrete external blockers, leave affected work unchecked, and continue independent eligible work; do not ask for renewed authorization. Never print secret values. After three failures with the same root cause, stop speculative fixes for that cause, record evidence and a minimal reproduction, and continue only independent eligible work. If no authorized useful work remains, ask a human and checkpoint as blocked. Host/tool/usage limits override the duration target: checkpoint truthfully, do not bypass limits, and resume only when execution is available again.
```

## Current user revision — 2026-09-21 (authoritative over the original launch text above)

The completed Tasks 1–16 describe the first release. The user now requests a simulator-first revision (Task17): remove the owner start mode and separate first-step/response-practice navigation, add a complete scored quiz with graphic customer/POS and an interview test mode, use confirmed content in the supplied GS25 training-map Markdown for stages and Gemini Q&A, and narrow owner UI to practice records/scores and checklist settings. Owner-authored rules are supplemental. The user's explicit parallel-agent instruction overrides the original sequential-only implementation approach for this coherent revision. Public manual summaries may now be runtime grounding; absent bodies and current policies must not be invented. Transaction values and POS graphics remain synthetic. Prior 420-minute contract has already been evaluated as completed-before-duration-target; continue measuring actual revision work without counting the stopped interval.

The subsequent screenshot-reference request is Task18: redesign all working pages around the supplied cyan/white rounded mobile-app references. Keep Task17 behavior and runtime evidence requirements; use original graphics instead of copying official logos or characters. Match desktop and mobile styles and verify fixed-navigation clearance in an actual browser.

Task19 is the latest authoritative request: rename to GStep; expand confirmed manual coverage, subjective Gemini grading and capped time scoring, add Gemini chat practice, helpful general/manual Q&A distinction, and Supabase authentication/role/store-scoped persistence. These explicit changes supersede prior local-only, deterministic-only and unsupported-answer refusal constraints. Preserve evidence provenance and enforce access controls. Continue independent implementation if intended Supabase connection settings are missing, record that external gate honestly and do not invent a live success.

Task20 is the current user request: directly walk through every course and scenario using Computer Use, audit confusing wording and inconsistent roles against the supplied manual, correct scenario-specific missions and feedback, retain36steps and preserve records, then verify and deploy.

Task21 is the current authoritative request: make checklist progress date-specific with selectable history, turn the signed-in owner experience into a complete store overview (account, crew questions, practice/test/chat scores, checklist authoring and completion), remove provider-specific model names from user-facing copy, expose AI response practice beside practice and hiring test from the simulator entry, and automatically conclude customer chat after a bounded natural exchange before coaching. Preserve existing histories and enforce store/role access in Supabase.

Task22 is the current visual request: replace the raised simulator icon in the mobile LNB with a symbol that reads as interactive practice instead of a generic grid, while preserving navigation behavior, sizing and active-state styling.

Task23 is the current access-state correction: a signed-out visitor may open the 경영주 관리 entry from the landing page, but must see only a plain explanation and login action. Local guest practice, questions, checklist progress, scores and other data must never appear as owner store data.
