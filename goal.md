# Ralph `/goal` 원문 후보

> 행사 도구에 실제 입력한 뒤에는 이 파일을 그 원문과 정확히 일치시키고, 변경 이유를 `progress.md`에 기록한다.

```text
목표: 신입 스토어 매니저가 첫 출근 전에 2+1 행사 고객 응대를 연습하고, 근무 중에는 같은 매장 규칙에 근거한 AI Q&A와 체크리스트를 사용하며, 경영주는 교육·질문·업무 진행 현황과 매뉴얼 보완 필요 사항을 확인할 수 있는 실제 작동 웹앱 MVP를 완성한다.

최종 결과 상태: 경영주가 가상 매장 규칙과 체크리스트를 저장하면, 신입 스토어 매니저가 AI 고객의 2+1 문의를 응대하고 가상 POS를 조회하며, 행동 로그에 근거한 피드백과 재도전을 거친다. 이후 근무 모드에서 동일 규칙 기반 Q&A와 체크리스트를 사용하고, 경영주 대시보드에서 그 결과를 확인한다. 규칙 수정은 이후 새 Q&A와 새 시뮬레이션에 반영되어야 한다.

위임: 먼저 AGENTS.md, prd.md, task.md, progress.md와 현재 코드를 읽어라. task.md에서 의존성이 충족된 가장 높은 우선순위의 미완료 작업 하나를 선택한다. 필요한 구현과 테스트를 수행하고 테스트·빌드·브라우저 검증을 실제로 실행한다. 실패하면 원인을 진단하고 안전한 범위에서 수정 후 재검증한다. 검증된 결과만 task.md와 progress.md에 기록하고 다음 작업으로 진행한다. 이미 충족한 기능을 다시 구현하지 말고 P0 수직 흐름 완성에 집중한다.

핵심 제약: 실제 GS25 업무 데이터·POS·사내 시스템·개인정보를 사용하지 않고 합성 데이터만 사용한다. Gemini API 키는 서버에서만 읽고 브라우저·로그·Git에 노출하지 않는다. LLM은 자연어 표현만 담당하며 상품·행사 사실과 행동 판정은 구조화 데이터와 결정론적 코드가 담당한다. 매뉴얼에 근거가 없는 답변은 만들어내지 않는다. 체크리스트와 대시보드를 감시·인사 평가 도구로 만들지 않는다. 범위를 P1·P2로 확장하기 전에 모든 P0 수용 기준을 만족한다.

완료 기준: (1) 경영주 규칙 저장부터 마지막 대시보드까지 한 브라우저 세션에서 시연 가능 (2) POS 조회 유무에 따라 행동 로그와 피드백이 실제로 달라짐 (3) 가상 POS·검증기·Q&A가 동일한 규칙을 사용 (4) 규칙 수정이 이후 새 세션에 반영 (5) 체크리스트가 새로고침 후 유지되고 대시보드에 집계 (6) 근거 없는 질문은 미해결로 기록 (7) Gemini 키나 API가 없어도 명시된 데모 모드로 시연 가능 (8) 단위·통합·핵심 E2E 테스트와 프로덕션 빌드 통과 (9) README에 실행법·데모 동선·합성 데이터·한계 기재.

검증 기록: 사용한 명령, 통과·실패 결과, 실패 후 수정 내용, 브라우저에서 확인한 동선, 남은 제약을 progress.md에 누적한다. 통과하지 않은 테스트나 확인하지 않은 배포 상태를 통과했다고 보고하지 않는다. 실제 Codex JSONL 세션 로그를 보존하고 사후에 내용을 만들어 넣지 않는다.

중단 및 에스컬레이션: 모든 완료 기준을 충족하면 종료한다. API 키·로그인·외부 서비스 권한, 유료 비용, 실제 데이터 접근, 파괴적 작업, 반복되는 환경 장애가 필요하면 임의로 우회하지 말고 progress.md에 원인과 안전한 대안을 기록하고 사람에게 요청한다. 같은 실패가 세 번 반복되면 추가 추측 구현을 멈추고 증거와 최소 재현을 남긴다.
```
